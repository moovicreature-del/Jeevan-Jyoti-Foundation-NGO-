# Server and API configuration
PORT=3000
GEMINI_API_KEY=

# Automated Email Dispatch Service (Optional SMTP Configuration)
# Example Gmail App Password, SendGrid, Amazon SES, or custom SMTP server
SMTP_HOST=
SMTP_PORT=
SMTP_SECURE=
SMTP_USER=
SMTP_PASS=
FROM_EMAIL=
